{
  "title": "Add Vitest",
  "description": "Add Vitest to an existing project",
  "cli": "nx",
  "type": "object",
  "properties": {
    "projectName": {
      "type": "string",
      "description": "The name of the project to add vitest to",
      "aliases": [
        "project"
      ]
    },
    "convert": {
      "type": "boolean",
      "description": "Convert the project to use Vitest if it's already using jest"
    }
  },
  "required": [
    "projectName"
  ]
}
